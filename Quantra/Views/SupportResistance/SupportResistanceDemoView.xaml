<UserControl x:Class="Quantra.Views.SupportResistance.SupportResistanceDemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quantra.Views.SupportResistance"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Quantra;component/Styles/EnhancedStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Support/Resistance Levels Demo" Style="{StaticResource EnhancedHeaderTextBlockStyle}" VerticalAlignment="Center"/>
            <ComboBox x:Name="SymbolComboBox" Width="150" Margin="20,0,0,0" SelectionChanged="SymbolComboBox_SelectionChanged" Style="{StaticResource EnhancedComboBoxStyle}">
                <ComboBoxItem Content="AAPL" IsSelected="True"/>
                <ComboBoxItem Content="MSFT"/>
                <ComboBoxItem Content="AMZN"/>
                <ComboBoxItem Content="NVDA"/>
                <ComboBoxItem Content="GOOG"/>
            </ComboBox>
            <ComboBox x:Name="TimeframeComboBox" Width="100" Margin="10,0,0,0" SelectionChanged="TimeframeComboBox_SelectionChanged" Style="{StaticResource EnhancedComboBoxStyle}">
                <ComboBoxItem Content="1day" IsSelected="True"/>
                <ComboBoxItem Content="1hour"/>
                <ComboBoxItem Content="15min"/>
            </ComboBox>
            <Button Content="Refresh Data" Margin="10,0,0,0" Click="RefreshButton_Click" Style="{StaticResource EnhancedButtonStyle}"/>
            <ToggleButton x:Name="SettingsToggleButton" Content="Settings" Margin="10,0,0,0" Click="SettingsToggleButton_Click"/>
        </StackPanel>
        
        <!-- Chart -->
        <lvc:CartesianChart Grid.Row="1" Grid.Column="0" 
                           x:Name="PriceChart" 
                           DisableAnimations="True"
                           Margin="10">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Date" Labels="{Binding DateLabels}">
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Price" Position="LeftBottom"></lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
        
        <!-- Splitter -->
        <GridSplitter Grid.Row="1" Grid.Column="1" Width="5" VerticalAlignment="Stretch" HorizontalAlignment="Center"/>
        
        <!-- Settings Panel -->
        <local:SupportResistanceConfigControl Grid.Row="1" Grid.Column="2" 
                                            x:Name="ConfigControl" 
                                            Margin="10"
                                            Visibility="Visible"/>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready" Style="{StaticResource EnhancedTextBlockStyle}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>