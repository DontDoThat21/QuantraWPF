<Window x:Class="Quantra.Views.StockExplorer.HorizontalLineDialog"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Price Level"
        Height="200"
        Width="400"
        Background="#23233A"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Add Horizontal Line"
                   Foreground="Cyan"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Price Level:"
                       Foreground="White"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="PriceLevelTextBox"
                     Background="#2D2D4D"
                     Foreground="White"
                     BorderBrush="#3E3E56"
                     Padding="5"
                     FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Label (optional):"
                       Foreground="White"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="LabelTextBox"
                     Background="#2D2D4D"
                     Foreground="White"
                     BorderBrush="#3E3E56"
                     Padding="5"
                     FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Add"
                    Click="AddButton_Click"
                    Style="{StaticResource EnhancedButtonStyle}"
                    Width="80"
                    Margin="0,0,10,0"/>
            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Style="{StaticResource EnhancedButtonStyle}"
                    Width="80"/>
        </StackPanel>
    </Grid>
</Window>
