<UserControl x:Class="Quantra.Controls.Components.PredictionHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Quantra.Controls.Components"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Quantra;component/Styles/EnhancedStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Title and Controls - Auto Mode Toggle -->
    <Grid Margin="0,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Prediction Analysis" 
                   Style="{StaticResource EnhancedHeaderTextBlockStyle}" Margin="5,0,0,10"/>

        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <!-- Auto Mode Toggle -->
            <materialDesign:Card Style="{StaticResource EnhancedContentCardStyle}" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Auto Mode" Style="{StaticResource EnhancedTextBlockStyle}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ToggleButton x:Name="AutoModeToggle" Width="40" Height="20"
                                  Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                  IsChecked="{Binding IsAutomatedMode, Mode=TwoWay}" 
                                  ToolTip="Enable/Disable automated analysis at regular intervals"/>
                </StackPanel>
            </materialDesign:Card>
            
            <Button x:Name="RefreshButton" Content="↻" Width="40" Height="30" 
                    Click="RefreshButton_Click"
                    Style="{StaticResource EnhancedButtonStyle}" Margin="0,0,5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
