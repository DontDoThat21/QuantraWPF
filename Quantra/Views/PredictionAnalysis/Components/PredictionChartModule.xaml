<UserControl x:Class="Quantra.Views.PredictionAnalysis.Components.PredictionChartModule"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:converters="clr-namespace:Quantra.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Resources>
        <!-- Resources are now defined globally in App.xaml -->
    </UserControl.Resources>

    <Border BorderBrush="{DynamicResource SystemBaseMediumColor}" 
            BorderThickness="1" 
            CornerRadius="4"
            Margin="10" 
            MinWidth="500">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Chart Header -->
            <Grid Grid.Row="0" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="{Binding Symbol}"
                           FontSize="18"
                           FontWeight="Medium"
                           VerticalAlignment="Center"/>
                
                <ComboBox Grid.Column="1" 
                          ItemsSource="{Binding Timeframes}"
                          SelectedItem="{Binding Timeframe, Mode=TwoWay}"
                          Width="80"
                          Margin="10,0,0,0"
                          Style="{StaticResource EnhancedComboBoxStyle}"
                          ToolTip="Select timeframe for chart data"/>
            </Grid>

            <!-- Price Chart -->
            <lvc:CartesianChart Grid.Row="1" 
                               Series="{Binding ChartSeries}"
                               DisableAnimations="True"
                               Hoverable="True"
                               DataTooltip="{x:Null}">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Labels="{Binding Labels}"
                             Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis LabelFormatter="{Binding YFormatter}"
                             Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
            
            <!-- Loading Indicator -->
            <Border Grid.Row="1" 
                    Background="#80000000" 
                    Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ProgressBar Width="30" 
                            Height="30"
                            IsIndeterminate="True"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</UserControl>