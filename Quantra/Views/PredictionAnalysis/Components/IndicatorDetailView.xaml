<UserControl x:Class="Quantra.Controls.Components.IndicatorDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quantra.Controls.Components"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400"
             Background="#262639">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" x:Name="IndicatorTitle" 
                       Text="Indicator History" 
                       Foreground="White" FontWeight="Bold" 
                       Margin="10,5"/>
            
            <Button Grid.Column="1" x:Name="CloseButton" 
                   Content="?" Width="24" Height="24" 
                   Background="Transparent" Foreground="White" 
                   BorderBrush="Transparent" Margin="5,5,10,5"
                   Click="CloseButton_Click"/>
        </Grid>
        
        <!-- Chart -->
        <lvc:CartesianChart Grid.Row="1" x:Name="IndicatorHistoryChart" 
                            Margin="10,5,10,10"
                            DisableAnimations="True">
            <lvc:CartesianChart.Series>
                <lvc:LineSeries Title="{Binding IndicatorName}" 
                                Values="{Binding HistoricalValues}" 
                                PointGeometry="{x:Null}" 
                                Fill="Transparent" 
                                Stroke="{Binding IndicatorColor}"
                                StrokeThickness="2"/>
            </lvc:CartesianChart.Series>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Date" Labels="{Binding DateLabels}"/>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="{Binding IndicatorName}"/>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</UserControl>
